#Eco blade ui

###Button Component
This is a reusable Button component built with React. It provides a basic button element that can be customized through props.

##Installation
To use this Button component in your project, first install it (if it's part of a package) or simply import it.

###bash
Copy code
npm install your-package-name
Or if it's a local component:

javascript
Copy code
import Button from './Button';
Usage
Below is a basic example of how to use the Button component:

##Customization
You can extend or modify the Button component by adding additional props or functionality as required.

##License
This project is licensed under the MIT License.
